# Deep Learning core
torch>=2.11.0
# torchvision>=0.20.0     # 暂时未使用：项目处理 CSI 张量，不涉及图像/视觉模型

# Channel simulation (TensorFlow backend)
tensorflow>=2.18.0        # Sionna 2.x depends on TF
sionna>=2.0.1
# dracopy>=0.2.0          # 暂时未使用：Sionna 可选几何依赖，当前 ray synthesizer 未用到

# Data / utilities
numpy>=2.4.4
h5py>=3.12.0
# scipy>=1.15.0           # 暂时未使用
pyyaml>=6.0.2
tqdm>=4.67.0
# matplotlib>=3.10.0      # 暂时未使用
# seaborn>=0.13.0         # 暂时未使用
# pandas>=2.2.0           # 暂时未使用

# Logging / experiment tracking
tensorboard>=2.18.0
# wandb>=0.19.0           # 暂时未使用：当前 logger 未集成 wandb

# Development
pytest>=8.3.0
# black>=24.10.0          # 暂时未使用：代码格式化工具，未配置 pre-commit/CI
# ruff>=0.8.0             # 暂时未使用：lint 工具，未配置 pre-commit/CI
