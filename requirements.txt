# Deep Learning core
torch>=2.11.0
torchvision>=0.20.0

# LLM ecosystem
transformers>=5.5.4
peft>=0.19.0
accelerate>=1.0.0
bitsandbytes>=0.45.0      # optional, for 8-bit loading
sentencepiece>=0.2.0      # Qwen tokenizer dependency
protobuf>=5.28.0

# Channel simulation (TensorFlow backend)
tensorflow>=2.18.0        # Sionna 2.x depends on TF
sionna>=2.0.1
dracopy>=0.2.0            # Sionna optional geometry dependency

# Data / utilities
numpy>=2.4.4
h5py>=3.12.0
scipy>=1.15.0
pyyaml>=6.0.2
tqdm>=4.67.0
matplotlib>=3.10.0
seaborn>=0.13.0
pandas>=2.2.0

# Logging / experiment tracking
tensorboard>=2.18.0
wandb>=0.19.0             # optional, disable in config if not used

# Development
pytest>=8.3.0
black>=24.10.0
ruff>=0.8.0
